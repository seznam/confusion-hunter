# Existující balíčky s verzemi
requests==2.31.0
numpy==1.24.3
pandas==2.1.1 --extra-index-url https://pypi.org/simple

# Neexistující balíčky nebo neexistující verze
unclaimed-package-a==0.1.0
unclaimed-package-b
unclaimed-package-c==9.9.9  # verze, která neexistuje

# Kombinace s překlepem nebo fiktivní názvy
unclaimed-package-d==2.25.1     # překlep v názvu
unclaimed-package-e==1.0
unclaimed-package-f

# Balíčky bez určení verze (mohou být nebo nemusí existovat)
flask
unclaimed-package-g
